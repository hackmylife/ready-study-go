# Why?

limit=2なら同時に走るworkは最大2です。入力ごとにgoroutineを無制限に作るのではなく、固定workerが次の仕事を受け取ります。

# Alternatives

semaphoreを使うならgoroutine起動前に枠を取得します。起動後に取得すると待機goroutine自体は無制限に増えます。

# Idiomatic Go

上限は共有資源の容量など具体的な制約から決めます。

# 振り返り

テストが示す具体例を一つ選び、入力から出力まで手で追ってください。境界値のテストを一つ自分で追加してから、もう一度解いてみましょう。
