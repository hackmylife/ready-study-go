# Why?

var c Counter; c.Add(2)で値は2です。初期化の制約がない型ではconstructorを削って公開APIを小さくできます。

# Alternatives

必須依存の注入や値の検証が必要になればconstructorに役割が生まれます。ゼロ値で有効な型まで一律にconstructorを作る必要はありません。

# Idiomatic Go

不要な生成関数を削ると、覚えるAPIと初期化手順が減ります。

# 振り返り

テストが示す具体例を一つ選び、入力から出力まで手で追ってください。境界値のテストを一つ自分で追加してから、もう一度解いてみましょう。
