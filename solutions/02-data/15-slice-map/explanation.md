# Why?

Mapは一要素から一要素を作ります。`[2,-3]`へstrconv.Itoaを適用すると`["2","-3"]`になります。型パラメータで型が変わる変換も表現できます。処理が一箇所だけなら具体的なforでも十分です。新しいsliceに格納しますが、要素がpointerなどの場合に参照先までコピーするわけではありません。

## 振り返り

入力例を一つ選び、各反復で変化する値と終了条件を説明してください。
