# Why?

iteratorへのrangeは、値を受け取る処理をyieldとして渡します。breakするとyieldがfalseを返し、生産側が終了できます。`[4,5,6]`から2件取る場合、5を追加した直後にbreakすれば6を要求しません。slices.Collectは全件を集める用途なので、ここでは途中で止めるrangeを使います。

## 振り返り

入力例を一つ選び、各反復で変化する値と終了条件を説明してください。
