# Why?

Mapなら`["Go is","fun"] → [["Go","is"],["fun"]]`、FlatMapなら`["Go","is","fun"]`になります。appendの`...`で一段だけ展開します。fが返した最初のsliceをそのまま出力にすると、共有された配列を後のappendで変更する可能性があるため、独立した出力へ追加します。参照型の要素の深いコピーまでは行いません。

## 振り返り

入力例を一つ選び、各反復で変化する値と終了条件を説明してください。
