# Why?

NewUser("Aki", 21) は User{Name:"Aki", Age:21} です。フィールド名付きリテラルで値の対応を明示します。

# Alternatives

この規模なら呼び出し元でUserリテラルを直接書けます。constructorを常に用意する必要はありません。Level 5で削除の判断も扱います。

# Idiomatic Go

フィールド名付きの初期化は宣言順への依存を減らします。

# 振り返り

テストが示す具体例を一つ選び、入力から出力まで手で追ってください。境界値のテストを一つ自分で追加してから、もう一度解いてみましょう。
