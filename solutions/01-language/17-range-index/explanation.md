# Why?

`for _, value := range values`のvalueは要素のコピーなので、`value *= 2`だけでは元のsliceは変わりません。`values[i] *= 2`なら`[2,-3,0] → [4,-6,0]`と呼び出し元も変わります。入力を保つ変換は02-data/06-slice-rangeで練習します。

## 振り返り

入力例を一つ選び、各反復で変化する値と終了条件を説明してください。
